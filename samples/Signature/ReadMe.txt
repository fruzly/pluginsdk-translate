1、FR_SignatureInfo中的 imagePath 设置为需要的签名图片；
2、FR_SignatureInfo中的 wsCertFile 设置为签名需要的证书；
2、FR_SignaturePosInfo中的FileSavePath设置为需要保存的文件路径。

/////////////////////////////////////////////////////////////////////////////
